# password-genrator
A lite and useable password genrater it's look simple and good i try to make most comptable ti understad it in 30 min project  

I create index.HTML then style.css then i connect css into index.HTML then create a title name it "random password generator - easy tutorials " then  i link it to css  

then go to body part i create div container create a h1 header thern name it genrate a <br> Random password then create a div display then create a input type text id password placeholder password then create a image copy.png then create a button then create a image generate.png alt generate password then create a script tag then create a const passwordbox equal document.getElementById("password") then create a const length = 12 then create a const uppercase = "ABCDEFGHIJKLMNOPQRSTUVWXYZ" then create a const lowercase = "abcdefghijklmnopqrstuvwxyz" then create a const number = "0123456789" then create a const symbol = "!@#$%^&*()_+~`|}{[]:;?><,./-=" then create a const allChars = uppercase + lowercase + number + symbol then create a function createPassword() { let password = "" password += uppercase[Math.floor(Math.random() * uppercase.length)] password += lowercase[Math.floor(Math.random() * lowercase.length)] password += number[Math.floor(Math.random() * number.length)] password += symbol[Math.floor(Math.random() * symbol.length)] while(length > password.length) { password += allChars[Math.floor(Math.random() * allChars.length)] } passwordBox.value = password }  function copyPassword() { passwordBox.select() document.execCommand("copy") }
